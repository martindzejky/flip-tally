# Flip Tally — no environment variables
#
# This app does not import from `$env/static/*`. A `.env` file is optional and not
# required for local development, CI, or Railway deployment.
#
# If you add features that need secrets or config, document each key here with a
# non-empty placeholder value, then run `pnpm sync`.
